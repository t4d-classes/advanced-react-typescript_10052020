import React from "react";
import ReactDOM from "react-dom";

// Be sure to read the comments in the "public/index.html" file and the
// "App.tsx" file. You may place the CSS from the "style" block in the
// "public/index.html" file in the "App.css" file

// Uncomment the code below to render the component tree

// import { App } from "./App";

// ReactDOM.render(<App />, document.getElementById("root"));
